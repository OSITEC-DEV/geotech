<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

        <record id="consultant_report_rule" model="ir.rule">

            <field name="name">Consultant Report</field>

            <field name="model_id" ref="model_lims_report"/>

            <field name="groups" eval="[(6,0, [ref('lims.group_lims_consultant')])]"/>

            <field name="perm_read" eval="True"/>

            <field name="perm_write" eval="True"/>

            <field name="perm_create" eval="False"/>

            <field name="perm_unlink" eval="False"/>

            <field name="domain_force">[('consultant_id','=',user.id),
                ('state', '!=', 'cancel')]
            </field>
        </record>

        <record id="manager_report_rule" model="ir.rule">

            <field name="name">Allow access to report</field>

            <field name="model_id" ref="model_lims_report"/>

            <field name="groups" eval="[(6,0, [ref('lims.group_lims_manager')])]"/>

            <field name="perm_read" eval="True"/>

            <field name="perm_write" eval="True"/>

            <field name="perm_create" eval="True"/>

            <field name="perm_unlink" eval="False"/>

            <field name="domain_force">[(1,'=',1)]</field>
        </record>

        <record id="admin_report_rule" model="ir.rule">

            <field name="name">Allow full access to report</field>

            <field name="model_id" ref="model_lims_report"/>

            <field name="groups" eval="[(6,0, [ref('base.group_no_one')])]"/>

            <field name="perm_read" eval="True"/>

            <field name="perm_write" eval="True"/>

            <field name="perm_create" eval="True"/>

            <field name="perm_unlink" eval="True"/>

            <field name="domain_force">[(1,'=',1)]</field>
        </record>
    </data>
</odoo>
